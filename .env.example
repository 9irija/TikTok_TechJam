# Copy this file to .env and fill in a real key. .env is gitignored -- never
# commit an actual API key.
#
# Phase 4's Research Strategist (agent/llm_client.py) uses Google's Gemini
# API (free tier) -- get a key at https://aistudio.google.com/apikey
GEMINI_API_KEY=
