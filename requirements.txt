# Phase 0 + P1 (agent/, run.py, run_p1.py): numpy only, matching the starter
# kit's own zero-dependency philosophy ("numpy only, no torch / pandas /
# scikit-learn"). Evaluator, convergence, FM, DeepFM, FM_BPR, orchestrator,
# recovery, research map, diagnosis, multi-fidelity, selector, logging,
# submission validator import nothing beyond this line.
numpy>=1.24

# ---------------------------------------------------------------------------
# Already installed in this dev environment, used for ad-hoc EDA notebooks /
# scripts only -- never imported by anything under agent/ or run.py itself.
pandas>=2.0

# ---------------------------------------------------------------------------
# Phase 4 only (agent/llm_client.py, agent/research_strategist.py,
# run_p4.py) -- the LLM Research Strategist. Everything else in this repo
# runs without these installed.
google-genai>=1.0    # Gemini API client (free tier -- see CLAUDE.md's provider decision)
python-dotenv>=1.0    # loads GEMINI_API_KEY from .env (gitignored; .env.example is the template)

# ---------------------------------------------------------------------------
# P2 Extended Model Zoo, first real use: agent/model_zoo/deepfm_mtl.py
# (multi-task DeepFM with auxiliary is_like/is_follow/is_comment/is_forward
# heads -- see docs/P2_FEATURES_AND_RESULTS.md). CPU-only wheel; autograd is a
# genuine win here over hand-rolled backprop (unlike FM/DeepFM/FM_BPR, which
# stay numpy-only on purpose -- see their own docstrings) because a 5-headed
# shared-bottom network's backward pass is exactly what autograd is for.
# deepfm_mtl_v1 (3-seed verified) is the current project-best.
torch>=2.0

# ---------------------------------------------------------------------------
# Used for one standalone diagnostic check (`lgbm_baseline` in the Research
# Map, see docs/P2_FEATURES_AND_RESULTS.md), NOT part of the core pipeline --
# no agent/model_zoo/ entry, no agent/experiment.py integration (LightGBM
# manages its own boosting/early-stopping internally, a real interface
# mismatch with every other model's per-epoch SGD loop -- not worth that
# refactor once the standalone result showed a clear, structurally-explained
# loss vs. the embedding-based models). Real result, not required to
# reproduce anything else in this repo.
lightgbm>=4.0

# ---------------------------------------------------------------------------
# P2 Hyperparameter Search (agent/hpo.py, tools/run_hyperparameter_search.py).
# Sits entirely outside the modeling/scoring logic -- Optuna only ever picks
# which hyperparameters agent/experiment.py's run_experiment (the same
# function every other candidate in this project runs through) should try
# next; it never sees .test, same train/valid/test discipline as everywhere
# else. The one place in this project's own limitations list where reaching
# for an existing tool was unambiguously correct instead of hand-rolling.
optuna>=3.0

# ---------------------------------------------------------------------------
# Not yet installed / not yet required. Uncomment as later phases need them
# (see CLAUDE.md roadmap). Keep this list honest -- an unused heavy
# dependency here is a Feasibility & Practicality red flag, not a convenience.
#
# scikit-learn>=1.3      # only if a future model actually needs its API --
                          # lightgbm's own native API above needed no sklearn
# matplotlib>=3.7          # only if a static PNG chart is ever needed instead
                          # of the HTML dashboard (tools/generate_dashboard.py
                          # is self-contained, no plotting library required)
