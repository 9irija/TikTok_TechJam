# Phase 0 + P1 (agent/, run.py, run_p1.py): numpy only, matching the starter
# kit's own zero-dependency philosophy ("numpy only, no torch / pandas /
# scikit-learn"). Evaluator, convergence, FM, DeepFM, FM_BPR, orchestrator,
# recovery, research map, diagnosis, multi-fidelity, selector, logging,
# submission validator import nothing beyond this line.
numpy>=1.24

# ---------------------------------------------------------------------------
# Already installed in this dev environment, used for ad-hoc EDA notebooks /
# scripts only -- never imported by anything under agent/ or run.py itself.
pandas>=2.0

# ---------------------------------------------------------------------------
# Phase 4 only (agent/llm_client.py, agent/research_strategist.py,
# run_p4.py) -- the LLM Research Strategist. Everything else in this repo
# runs without these installed.
google-genai>=1.0    # Gemini API client (free tier -- see CLAUDE.md's provider decision)
python-dotenv>=1.0    # loads GEMINI_API_KEY from .env (gitignored; .env.example is the template)

# ---------------------------------------------------------------------------
# Not yet installed / not yet required. Uncomment as later phases need them
# (see CLAUDE.md roadmap). Keep this list honest -- an unused heavy
# dependency here is a Feasibility & Practicality red flag, not a convenience.
#
# torch>=2.0            # P2 Extended Model Zoo (DCNv2 etc.) if/when it needs autograd
# scikit-learn>=1.3      # P2 LightGBM's sklearn API + light preprocessing
# lightgbm>=4.0          # P2 Extended Model Zoo
# optuna>=3.0             # P2 Hyperparameter Search
# matplotlib>=3.7          # only if a static PNG chart is ever needed instead
                          # of the HTML dashboard (tools/generate_dashboard.py
                          # is self-contained, no plotting library required)
