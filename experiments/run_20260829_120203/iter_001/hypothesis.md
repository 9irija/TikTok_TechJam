# iter_001 -- deepfm_bpr_v1_regularized

## Hypothesis
Directly acts on deepfm_bpr_v1's own diagnosis, same pattern that worked for fm_bpr_default->fm_bpr_regularized and deepfm_default->deepfm_regularized: its overfitting_risk flag showed the best validation epoch landing at ~20% of training length -- DeepFM's extra capacity combined with BPR's pairwise sampling overfits even faster than plain FM_BPR did. L2 raised another 10x (1e-4 -> 1e-3, on top of the L2 already inherited from deepfm_regularized) and patience tightened (default 4 -> 2) to stop closer to where validation actually peaks.

## Notes
Generated by _diagnosis_driven_candidates() because deepfm_bpr_v1's own insight flagged overfitting_risk, same reactive pattern as fm_bpr_regularized.

## Diff vs previous iteration
```json
{
  "__root__": true
}
```
