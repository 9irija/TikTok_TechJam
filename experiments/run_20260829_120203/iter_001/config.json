{
  "id": "deepfm_bpr_v1_regularized",
  "model": "deepfm_bpr",
  "hypothesis": "Directly acts on deepfm_bpr_v1's own diagnosis, same pattern that worked for fm_bpr_default->fm_bpr_regularized and deepfm_default->deepfm_regularized: its overfitting_risk flag showed the best validation epoch landing at ~20% of training length -- DeepFM's extra capacity combined with BPR's pairwise sampling overfits even faster than plain FM_BPR did. L2 raised another 10x (1e-4 -> 1e-3, on top of the L2 already inherited from deepfm_regularized) and patience tightened (default 4 -> 2) to stop closer to where validation actually peaks.",
  "hyperparams": {
    "k": 16,
    "l2": 0.001,
    "hidden": [
      128,
      64
    ],
    "lr": 0.001,
    "patience": 2
  },
  "fields": [
    "user_id",
    "video_id",
    "author_id",
    "tab",
    "dur_bucket"
  ],
  "parent_id": "deepfm_bpr_v1",
  "seeds": [
    0
  ],
  "notes": "Generated by _diagnosis_driven_candidates() because deepfm_bpr_v1's own insight flagged overfitting_risk, same reactive pattern as fm_bpr_regularized.",
  "edge_type": "debug"
}